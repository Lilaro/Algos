// Take in an array of 2 numbers or just start by declaring an array of 2 numbers
//  starting at 0
// Take in a length?
// Add 2 (index 0 & 1) numbers and push that sum onto the array
// Add index 1 & 2 and push that sum onto the array etc...
// Return the array



function Fibonacci(len) {
    let arr = [0, 1]
    for (let i = 0; i <= len; i++) {
        
    }
}